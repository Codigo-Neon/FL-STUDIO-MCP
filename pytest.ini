[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --no-header -m "not slow"
markers =
    slow: tests opcionales que tardan o requieren recursos reales
